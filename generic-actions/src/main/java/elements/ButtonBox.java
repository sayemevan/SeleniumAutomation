package elements;

import org.openqa.selenium.WebElement;

public class ButtonBox extends TypifiedElements {
    protected ButtonBox(WebElement wrappedElement) {
        super(wrappedElement);
    }
}
