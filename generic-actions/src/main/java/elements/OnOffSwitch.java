package elements;

import org.openqa.selenium.WebElement;

public class OnOffSwitch extends TypifiedElements {
    protected OnOffSwitch(WebElement wrappedElement) {
        super(wrappedElement);
    }
}
