package elements;

import org.openqa.selenium.WebElement;

public class PushButton extends TypifiedElements {
    protected PushButton(WebElement wrappedElement) {
        super(wrappedElement);
    }
}
