package com.ababil.generic.actions.elements;

import org.openqa.selenium.WebElement;

public class TextBox extends TypifiedElements {
    protected TextBox(WebElement wrappedElement) {
        super(wrappedElement);
    }
}
